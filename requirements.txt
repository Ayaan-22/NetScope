# NetScope — Python Dependencies
# Core (required)
python-nmap>=0.7.1      # Nmap Python wrapper (optional at runtime, gracefully degrades)

# Configuration
PyYAML>=6.0.1           # YAML config file support

# Web dashboard
fastapi>=0.115.0        # Dashboard API server
uvicorn>=0.30.0         # ASGI server for netscope-dashboard

# Optional enrichment
# shodan>=1.31.0        # Uncomment to enable Shodan integration
